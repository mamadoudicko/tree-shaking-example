# tree-shaking

## Building and running on localhost

Install dependencies:

```sh
pnpm install
```

Create a production build (dist folder):

```sh
pnpm run build-prod
```

Run

```sh
node dist/bundle.js
```
